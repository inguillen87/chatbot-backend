[pytest]
testpaths = tests
addopts = -m "not legacy"
markers =
    legacy: tests en cuarentena (excluidos por defecto)
    integration: toca servicios externos
    contract: valida el schema del payload
