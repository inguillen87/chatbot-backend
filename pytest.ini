[pytest]
testpaths = tests
markers =
    quarantine: marks tests that are temporarily quarantined pending fixes
    external_network: allows external network only when CHATBOC_ALLOW_EXTERNAL_NETWORK_TESTS=1 is also set
