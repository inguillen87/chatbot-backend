# Auto detect text files and perform LF normalization
* text=auto

# The official snapshot is byte-hashed; checkout must not rewrite its newline.
data/municipios/junin/official_department_boundary.geojson text eol=lf


